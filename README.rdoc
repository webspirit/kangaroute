== INSTALATION

* Ruby version: 2.1.1

* Change username & password at /config/database.yml.example file & save it as /config/database.yml

* Run <tt>bundle</tt>

* And then <tt>rake db:create db:migrate db:seed</tt>
